const addButton = document.getElementById("addButton");

addButton.addEventListener("click", addGoal);

function addGoal() {
  // This function will be implemented to handle adding new items
  // You can prompt the user for details and create new elements in the items section
  alert("This functionality is not yet implemented. Click OK to dismiss.");
}
